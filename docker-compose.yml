services:
  redis:
    image: redis:7-alpine
    container_name: my-redis-instance
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped


# make a request every 2 minutes to cahce process url
  
volumes:
  redis_data: